// Package keys owns API-key source loading, snapshot composition, lookup, and
// request authentication. sources report contributions; only Store can publish
// a resident lookup snapshot.
package keys
